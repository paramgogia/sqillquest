import { Root as AspectRatioPrimitiveRoot } from "@radix-ui/react-aspect-ratio";

// We assign the imported and renamed component to the final export name.
const AspectRatio = AspectRatioPrimitiveRoot;

export { AspectRatio };